<% flash.each do |type, message| %>
  <div id="flash" class="<%= type == 'notice' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' %> px-4 py-3 rounded relative" role="alert">
    <span class="block sm:inline"><%= message %></span>
  </div>
<% end %> 